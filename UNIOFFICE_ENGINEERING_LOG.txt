# UNIOFFICE ENGINEERING LOG

Date: 2026-09-19 (session started 19:43 IST, +05:30)
Objective: Production go-live and local AI/NPU investigation

This file is the plain-text record of this session's work. It holds no
secrets: environment variables are named, never valued.

Timestamps: every clock time in this log is anchored to git commit times,
which are authoritative. An earlier revision of this file carried
estimated times (for example "21:05 - 21:40") that were wrong; they were
corrected once ONNX Runtime's own log showed the real clock at 20:37.


==============================================================================
CURRENT STATE
==============================================================================

Repository
  Path ............ D:\personal projects\unioffice
  Branch .......... main, tracking origin/main
  HEAD ............ 5500720 "write down what the api and worker need in production"
  origin/main ..... 5500720 (in sync at session start)
  Working tree .... clean at session start
  Git identity .... Madhavan <175943252+MAD1IZJOD@users.noreply.github.com>
                    (repo-local user.name / user.email already correct)

Runtime at session start (started by the user, not by this session)
  API ............. node PID 27716, started 19:23:31, listening 127.0.0.1:4000
  Worker .......... node PID 7192, started 19:24:28, no listener (expected)
  Ollama .......... PID 17192, listening 127.0.0.1:11434
  cloudflared ..... Windows service, Running, Automatic
  WSAIFabricSvc ... Stopped, Manual (changed by the user before this session;
                    this session does not re-enable it)
  Free RAM ........ 8.6 GB of 23.3 GB

Ports 4000 and 11434 bind 127.0.0.1 only. Cloudflare Tunnel is the only
public path to the API.


==============================================================================
COMPLETED
==============================================================================

Phase A - Baseline (19:43)
  - Git identity, branch, remote, sync and clean tree verified.
  - Runtime identified: user-started API (PID 27716) and worker (PID 7192),
    Ollama on 127.0.0.1:11434, cloudflared service running.

Phase B - Quality gate (19:43 - before 19:59)
  - Frozen-lockfile install, typecheck, lint, tests and build all green on
    HEAD 5500720 without any change (see TEST RESULTS).

Phase C - Runtime (before 19:59)
  - Local and public /health and /readiness answer; public and local uptime
    differ by one second, so the tunnel reaches the same process.

Phase D - Real end-to-end missions (before 19:59, then 20:01 - 20:09)
  - Measured planning through Cloudflare three times (see LOCAL RUNTIME).
  - Found and fixed a real defect in how missions start (see CHANGES MADE).
  - Re-ran the mission end to end through the public URL on the fix.

Phase E - Deployment (after 20:09, before 20:17)
  - Vercel CLI is not logged in on this machine, so the Vercel project could
    not be inspected or changed from here (see MANUAL ACTIONS REQUIRED).
  - Read-only checks of both domains (see DEPLOYMENT STATE).
  - Found and fixed a launch blocker: the live API refused the production
    origin (see CHANGES MADE, item 3).

Phase F - Security (20:09 - 20:29)
  - Live authorization checks, launch permission checks, CORS, database
    posture, secrets scan of tracked files and all history, browser bundle.
  - A permission probe exposed a real race in the new launch path (a
    cancelled mission ran to completion). Fixed in two steps and verified
    live after the plans had time to finish (see CHANGES MADE, item 2).

==============================================================================
CHANGES MADE
==============================================================================

1. Missions are planned and queued on the server in one step.

   Problem, reproduced rather than assumed:
     The room started a mission with two browser requests: POST /plan, held
     open for the whole plan (measured 102-118 s), then POST /execute. If
     the browser did not complete the second request - tab closed, laptop
     asleep, connection dropped, a proxy giving up on a long request - the
     mission was left planned, status "queued", with ZERO execution jobs.
     The worker only claims jobs, so nothing would ever run it, while the
     status said it was waiting. Demonstrated on mission 3760ff44: planned,
     3 tasks pending, 0 jobs.

   Fix:
     - apps/api/src/mission-launcher.ts: plans then queues on the server,
       tracks launches per mission so one mission is never planned twice.
     - POST /work/:id/launch: same permission as running a mission
       (missions.operate, re-checked against current membership). Answers
       202 at once. 409 if already planning or already planned. 503 if the
       server has no launcher.
     - POST /work/:id/plan now refuses while a launch is in flight, so the
       older route cannot start a second plan beside it.
     - The room's autostart and replan-retry call launch instead of
       plan-then-execute.
     - Nothing else changed: planning, queueing, the worker, the queue
       schema and the database are untouched. No migration.

   This also removes the Cloudflare timeout exposure entirely: no request
   is now held open for longer than a couple of seconds.

2. A cancelled mission can no longer be revived by a launch.

   Found live, not in review: a permission probe cancelled a mission a
   moment after launching it. The mission ran to completion anyway.

   Cause: two stale whole-row writes. The cancel landed in the gap before
   the background plan marked the mission as planning, and the plan then
   wrote its own copy over the cancellation, finished, and queued it.

   Fix, in two commits:
     a) 0382397 - the launch route marks the mission as planning with a
        conditional write (WorkRepository.transitionStatus: update ... where
        status = 'queued') BEFORE answering 202. A cancel that got there
        first wins (launch gets 409); one that arrives after is refused,
        because cancelling a mission that is actively planning was already
        refused.
     b) 85705a5 - planWork no longer rewrites a mission a launch already
        marked as planning, and re-reads the mission after the model
        returns: if it was cancelled meanwhile, no steps are created and it
        is not put back to waiting. The launcher queues only a mission the
        plan left waiting.

   (a) alone was NOT enough. Re-running the race on 0382397 and waiting for
   the plans to finish showed a mission reported cancelled that still went
   on to complete. (b) closes it: re-running on 85705a5, all three cancelled
   missions stayed cancelled with 0 tasks and 0 jobs, checked after their
   plans would have finished.

3. The live API now accepts the production web origin (local config only).

   The API's CORS allow-list comes from API_CORS_ORIGINS. It was not set in
   the local .env, so it defaulted to localhost only: every browser request
   from https://unioffice.pro would have been refused. Added to the local
   .env (not committed, never tracked):
     API_CORS_ORIGINS = https://unioffice.pro, https://www.unioffice.pro,
                        http://localhost:5173, http://127.0.0.1:5173
   The localhost entries stay because this machine is also the development
   machine. Verified through the public URL: unioffice.pro and www allowed;
   unioffice.online and an arbitrary origin get no CORS header.

==============================================================================
COMMITS
==============================================================================

ede9f87  plan and queue a mission on the server in one step
d18f893  start missions with one request instead of two
0e6fdb3  start an engineering log for the go-live pass
0382397  don't let a launch undo a cancel
85705a5  keep a cancellation that lands while a plan is being written

Every commit: Author and Committer =
  Madhavan <175943252+MAD1IZJOD@users.noreply.github.com>
no trailers, pushed to origin/main immediately after committing.

==============================================================================
TEST RESULTS
==============================================================================

Baseline on HEAD 5500720, before any change (19:43 - 19:59)
  pnpm install --frozen-lockfile ..... up to date
  pnpm typecheck --force ............. 13/13 tasks passed
  pnpm lint --force .................. 1/1 passed (web; the only linted package)
  pnpm test --force .................. 11/11 tasks passed
      api 469, database 53, memory 48, connect 45, tools 38,
      orchestrator 37, governance 26, agents 26, skills 22, worker 0,
      web 134 (22 files)  -> 898 tests, 0 failures
  pnpm build --force ................. passed, main chunk 720.74 kB (209.08 kB gzip)

After the launch change (before commit d18f893, 20:01)
  api ................................ 478 passed, 0 failed (+9: 4 launcher, 5 route)
  web ................................ 136 passed, 0 failed (+2: launch client)
  web typecheck + eslint ............. clean

After the cancel-race fixes (before commit 85705a5, 20:24)
  api ................................ 488 passed, 0 failed
  database ........................... 53 passed, 0 failed (typecheck clean)

Live checks against the real stack
  Authorization (localhost:4000) ..... 17/17 passed
      no token and forged token -> 401 on /me, /skills, /work, /approvals,
      /members; owner of A -> 404 on B's skills, missions, members; viewer
      -> 403 writing a skill and inviting an owner; invented role -> 400;
      client-chosen system scope -> 400; unknown CORS origin -> no header.
  Launch permissions (public URL) .... no token 401, forged 401, viewer 403,
      owner of A launching B's mission 404 (both ways of naming it); the
      probe mission was not planned by any of them.
      Note: an earlier probe reported 202 for "another organization's
      owner". That account (test-b) is an active MEMBER of organization A,
      so 202 was correct. The probe was wrong, not the server.
  Cancel race (public URL) ........... on 85705a5: 3/3 rounds cancelled,
      0 tasks, 0 jobs, re-checked after the plans had time to finish.
  Database posture ................... supabase/checks/posture.sql: 0 rows
      (RLS enabled and forced everywhere, no browser-role privileges).
  Secrets ............................ .env never tracked and never in
      history; 0 credential-shaped strings in tracked files or in any line
      ever added to history; the tracked unioffice.md.docx scanned
      separately: none.
  Browser bundle (production values).. API URL and origin baked in, uses
      /launch, only the anon key (JWT role "anon"), no server-only names,
      no source maps.
  Exposure ........................... 4000 and 11434 bound to 127.0.0.1
      only; nothing of ours on 0.0.0.0.

==============================================================================
DEPLOYMENT STATE
==============================================================================

Observed read-only between 20:09 and 20:17. Nothing was changed on Vercel, Cloudflare DNS
or Supabase in this session.

  https://unioffice.online ...... 200, Vercel. The MARKETING site (title
                                  "UNIOFFICE - The AI-Native Operating
                                  System", waitlist; the same bundle it
                                  served on unioffice.pro before). It has
                                  no link to unioffice.pro yet.
  https://www.unioffice.online .. 200, Vercel, same site.
  https://unioffice.pro ......... 404, served by Cloudflare. The zone is on
                                  Cloudflare now (needed for the tunnel) and
                                  the apex has no record pointing at Vercel.
                                  The application is NOT live on this domain.
  https://www.unioffice.pro ..... 404, same.
  https://api.unioffice.pro ..... 200, Cloudflare Tunnel -> 127.0.0.1:4000.

  Vercel CLI .................... not logged in on this machine; the web
                                  project could not be inspected. No token
                                  was requested.

A production build made here with the production values behaves
correctly. That proves the code is ready. It does not prove Vercel has been
given the same values - that needs checking in the Vercel dashboard.

==============================================================================
LOCAL RUNTIME
==============================================================================

Endpoints (before 19:59)
  http://127.0.0.1:4000/health ....... 200 {"status":"ok","uptimeSeconds":1290}
  http://127.0.0.1:4000/readiness .... 200 supabase ready, ollama ready, queue ready
  https://api.unioffice.pro/health ... 200, uptimeSeconds 1291 (same process)
  https://api.unioffice.pro/readiness  200, all ready

Planning latency through Cloudflare (POST /work/:id/plan, test org A)
  Run                          Result
  model warm, 2 steps ........ 200 in 102.5 s
  model warm (earlier) ....... 200 in 114.1 s  (2026-09-19, previous session)
  model cold (ollama stop) ... 200 in 118.1 s
  -> Cloudflare's documented 100 s proxy timeout was NOT reproduced on this
     tunnel: all three runs over 100 s succeeded. The real limit is unknown
     and undocumented for this path, so relying on it was a risk; the fix
     above removes the dependence on it.

Full mission, old two-request path (before 19:59)
  plan 102.5 s, execute queued in 3.9 s, worker claimed in 1 s,
  completed at 190 s. financial-analysis v1 + calculator, 2 artifacts.

Full mission, new launch path, client gone after one request (between 20:01 and 20:09)
  POST /launch ............... 202 in 1.5 s
  second launch .............. 409 "already being planned"
  planning ................... 5 s -> 109 s
  queued by the server ....... 109 s
  worker claimed ............. 113 s
  completed .................. 322 s
  financial-analysis v1 + calculator:completed, 2 artifacts,
  knowledge.created, 23 events.

Model residency
  OLLAMA_KEEP_ALIVE is not set, so qwen3:8b unloads after 5 idle minutes and
  each cold start adds ~20 s of load time and needs ~6 GB free at that moment.

Process ownership
  The API was restarted just after 20:01 onto the new code: stopped by PID (27716 and
  its tsx launcher 28388 only), then started with
  `pnpm --filter @unioffice/api start` in its own visible PowerShell window
  titled "UNIOFFICE API", so it is not tied to this session.
  The worker (PID 7192) was not restarted: nothing in this change touches it.

==============================================================================
NPU INVESTIGATION
==============================================================================

(filled in during Phase G - secondary to go-live)


==============================================================================
KNOWN ISSUES
==============================================================================

Carried in from earlier sessions, to be re-verified today:

1. Planning runs synchronously inside POST /work/:id/plan. Measured at
   60-114 s. Cloudflare's documented proxy read timeout is 100 s, so this
   may return 524 to a browser. One run at 114.1 s did succeed through the
   tunnel on 2026-09-19. Needs measuring again before deciding anything.

2. qwen3:8b needs roughly 6 GB resident. On 2026-09-19 it failed to load
   with an out-of-memory error at ~2 GB free and succeeded at ~5.6 GB free.

3. /readiness reports ollama "ready" when the server answers /api/tags,
   even if the model cannot be loaded.


==============================================================================
DEFERRED WORK
==============================================================================

(filled in as decisions are made)


==============================================================================
MANUAL ACTIONS REQUIRED
==============================================================================

Each of these needs an account only the owner holds. None was done in this
session, and no credential was asked for.

1. Vercel - the web project built from apps/web
   Settings -> Environment Variables (Production):
     VITE_API_URL ........... https://api.unioffice.pro
     VITE_APP_ORIGIN ........ https://unioffice.pro
     VITE_SUPABASE_URL ...... the project URL
     VITE_SUPABASE_ANON_KEY . the anon key ONLY
   Then Deployments -> Redeploy. Vite bakes these in at build time, so an
   existing deployment keeps its old values until it is rebuilt.
   Never add SUPABASE_SERVICE_ROLE_KEY or any server-only value here.

2. Vercel - same project -> Settings -> Domains
   Add unioffice.pro and www.unioffice.pro. Vercel then shows the exact DNS
   records it wants.

3. Cloudflare - DNS for the unioffice.pro zone
   Add the records Vercel showed in step 2, for @ and www, exactly as shown.
   Leave api.unioffice.pro (the tunnel) untouched. Follow Vercel's domain
   page on the proxy setting; for a domain served by Vercel it normally
   asks for "DNS only" (grey cloud) so it can issue the certificate.

4. Supabase - Authentication -> URL Configuration
   Site URL ....... https://unioffice.pro
   Redirect URLs .. https://unioffice.pro/**, https://www.unioffice.pro/**,
                    http://localhost:5173/**
   Without this, Google and the email link complete and then return people
   to the old Site URL, which looks like sign-in silently failing.

5. Marketing site (separate Vercel project on unioffice.online)
   Add the "Enter UNIOFFICE" call to action pointing at https://unioffice.pro.

6. Optional, recommended - Ollama
   Set OLLAMA_KEEP_ALIVE (for example 24h) for the Ollama service and
   restart it, so qwen3:8b stays loaded. Unset, it unloads after 5 idle
   minutes and the next plan waits ~20 s to load it and needs ~6 GB free
   at that moment.

==============================================================================
FINAL PRODUCTION CHECKLIST
==============================================================================

(filled in at the end)
